import React from 'react'
import { TryIt } from '../style/TryItButton.styled'

const TryItButton = () => {
    return (
        <TryIt>
            <span>Try it free 7 days</span> then $20/mo. thereafter
        </TryIt>
    );
};

export default TryItButton;