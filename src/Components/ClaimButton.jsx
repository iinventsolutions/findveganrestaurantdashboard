import React from 'react'
import { ClaimBtn } from '../style/ClaimButton.styled'

const ClaimButton = () => {
    return (
        <ClaimBtn>
            GET STARTED
        </ClaimBtn>
    );
};

export default ClaimButton